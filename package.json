{
  "name": "hot-burgers",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "autoprefixer-stylus": "^1.0.0",
    "firebase": "8.2.6",
    "prop-types": "^15.7.2",
    "re-base": "^4.0.0",
    "react": "^16.10.2",
    "react-dom": "^16.10.2",
    "react-router-dom": "^5.1.2",
    "react-scripts": "^3.2.0",
    "react-transition-group": "^4.3.0",
    "stylus": "^0.54.7"
  },
  "scripts": {
    "dev": "react-scripts --openssl-legacy-provider start",
    "start": "react-scripts --openssl-legacy-provider build",
    "build": "react-scripts --openssl-legacy-provider build",
    "now-build": "react-scripts --openssl-legacy-provider build",
    "prebuild": "npm run styles",
    "styles": "stylus -u autoprefixer-stylus ./src/css/style.styl -o ./src/css/style.css",
    "styles:watch": "npm run styles -- -w",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "concurrently": "^5.0.0"
  }
}